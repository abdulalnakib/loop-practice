// Create an array of all phones and display the element of the array with while loop
var phones = ['Oneplus', 'Xiaomi', 'Apple', 'Samsung', 'Vivo'];
var i = 0;
while (i < phones.length) {
    console.log(phones[i]);
    i++;
}