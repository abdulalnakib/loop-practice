// Declare all the topics in an array that u learnt last few days and put it as an output
var topics = ['javascript', 'variables', 'numeric', 'boolean', 'string', 'and more'];

for (var i = 0; i < topics.length; i++) {
    var topic = topics[i];
    console.log(topic)
}